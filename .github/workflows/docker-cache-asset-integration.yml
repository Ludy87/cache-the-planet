name: Docker cache asset integration
on:
  workflow_call:
    inputs:
      save-cache:
        required: false
        type: boolean
        default: false
      restore-cache:
        required: false
        type: boolean
        default: false

env:
  CACHE_REPOSITORY: Ludy87/cache-the-planet
  CACHE_MANIFEST_BRANCH: cache-data
  FILES_TO_HASH: |
    examples/docker-cache/Dockerfile
    examples/docker-cache/hello.txt

permissions:
  contents: read

jobs:
  hash_pr:
    name: Hash PR Docker files
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      files_hash: ${{ steps.hash.outputs.files_hash }}
    steps:
      - name: Checkout PR revision for hashing only
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
      - name: Hash Docker files
        id: hash
        env:
          ZIZMOR_VALUE_1: ${{ hashFiles(env.FILES_TO_HASH) }}
        run: |
          HASH="$ZIZMOR_VALUE_1"
          echo "files_hash=$HASH" >> "$GITHUB_OUTPUT"
  save-cache:
    if: ${{ inputs.save-cache }}
    name: Build and save Docker cache asset
    needs: hash_pr
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha || 'main' }}
          persist-credentials: false

      - name: echo Dockerfile and context hash
        env:
          ZIZMOR_VALUE_2: ${{ needs.hash_pr.outputs.files_hash }}
        run: |
          echo "Dockerfile and context hash: $ZIZMOR_VALUE_2"

      - name: Restore Docker cache
        uses: ./
        with:
          repository: ${{ env.CACHE_REPOSITORY }}
          cache-name: docker
          scope: auto
          token: ${{ github.token }}
          key: ${{ needs.hash_pr.outputs.files_hash }}
          version: 2
          allow-shared-restore: true
          restore-keys: |
            linux-x64/
          path: examples/docker-cache/.cache/docker-buildx
          strict: false

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Build example image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: examples/docker-cache
          file: examples/docker-cache/Dockerfile
          tags: cache-the-planet-example:test
          push: false
          outputs: type=cacheonly
          cache-from: type=local,src=examples/docker-cache/.cache/docker-buildx
          cache-to: type=local,dest=examples/docker-cache/.cache/docker-buildx-new,mode=max

      - name: Replace local BuildKit cache
        shell: bash
        run: |
          rm -rf examples/docker-cache/.cache/docker-buildx
          mv examples/docker-cache/.cache/docker-buildx-new examples/docker-cache/.cache/docker-buildx

      - name: Save Docker cache asset
        # if: ${{ github.event.pull_request.head.repo.full_name == github.repository && success() }}
        if: ${{ success() }}
        id: save
        uses: ./save
        with:
          repository: ${{ env.CACHE_REPOSITORY }}
          cache-name: docker
          # auto saves an isolated PR cache on pull requests and a trusted
          # default-branch cache on main; the action creates the shared alias
          # only from the default branch when appropriate.
          scope: shared
          token: ${{ github.token }}
          key: ${{ needs.hash_pr.outputs.files_hash }}
          version: 2
          path: examples/docker-cache/.cache/docker-buildx
          strict: false
          allow-pr-cache: true

      - name: Upload Docker cache artifact for trusted publisher
        if: ${{ (steps.save.outputs.is_fork == 'true' || steps.save.outputs.read_only == 'true') && success() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: cache-the-planet-pr-${{ github.event.pull_request.number }}-docker-${{ needs.hash_pr.outputs.files_hash }}-v2
          path: examples/docker-cache/.cache/docker-buildx
          if-no-files-found: error
          retention-days: 1

  restore-cache:
    if: ${{ inputs.restore-cache }}
    name: Restore Docker cache asset in a fresh job
    needs: [hash_pr]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: main
          persist-credentials: false

      - name: Restore Docker cache asset - auto
        id: cache-auto
        uses: ./
        with:
          repository: ${{ env.CACHE_REPOSITORY }}
          cache-name: docker
          scope: auto
          key: ${{ needs.hash_pr.outputs.files_hash }}
          version: 2
          allow-shared-restore: true
          restore-keys: |
            linux-x64/
          path: examples/docker-cache/.cache/docker-buildx
          strict: true

      - name: Assert exact asset cache hit - trusted
        if: ${{ steps.cache-auto.outputs.matched-key != '' && startsWith(steps.cache-auto.outputs.matched-key, 'trusted/') }}
        shell: bash
        env:
          ZIZMOR_VALUE_3: ${{ steps.cache-auto.outputs.cache-hit }}
          ZIZMOR_VALUE_4: ${{ steps.cache-auto.outputs.matched-key }}
          ZIZMOR_VALUE_5: ${{ github.repository }}
          ZIZMOR_VALUE_6: ${{ needs.hash_pr.outputs.files_hash }}
        run: |
          test "$ZIZMOR_VALUE_3" = "true"
          test "$ZIZMOR_VALUE_4" = "trusted/$ZIZMOR_VALUE_5/main/docker/linux-x64/$ZIZMOR_VALUE_6/v2"

      - name: Assert exact asset cache hit start with shared
        if: ${{ steps.cache-auto.outputs.matched-key != '' && startsWith(steps.cache-auto.outputs.matched-key, 'shared/') }}
        shell: bash
        env:
          ZIZMOR_VALUE_7: ${{ steps.cache-auto.outputs.cache-hit }}
          ZIZMOR_VALUE_8: ${{ steps.cache-auto.outputs.matched-key }}
          ZIZMOR_VALUE_9: ${{ github.repository }}
          ZIZMOR_VALUE_10: ${{ needs.hash_pr.outputs.files_hash }}
        run: |
          test "$ZIZMOR_VALUE_7" = "true"
          test "$ZIZMOR_VALUE_8" = "shared/$ZIZMOR_VALUE_9/docker/linux-x64/$ZIZMOR_VALUE_10/v2"

      - name: Assert exact asset cache hit - untrusted
        if: ${{ steps.cache-auto.outputs.matched-key != '' && startsWith(steps.cache-auto.outputs.matched-key, 'untrusted/') }}
        shell: bash
        env:
          ZIZMOR_VALUE_11: ${{ steps.cache-auto.outputs.cache-hit }}
          ZIZMOR_VALUE_12: ${{ steps.cache-auto.outputs.matched-key }}
          ZIZMOR_VALUE_13: ${{ github.repository }}
          ZIZMOR_VALUE_14: ${{ github.event.pull_request.number }}
          ZIZMOR_VALUE_15: ${{ needs.hash_pr.outputs.files_hash }}
        run: |
          test "$ZIZMOR_VALUE_11" = "true"
          test "$ZIZMOR_VALUE_12" = "untrusted/$ZIZMOR_VALUE_13/pr-$ZIZMOR_VALUE_14/docker/linux-x64/$ZIZMOR_VALUE_15/v2"

      - name: Restore Docker cache asset - trusted
        id: cache-trusted
        uses: ./
        with:
          repository: ${{ env.CACHE_REPOSITORY }}
          cache-name: docker
          scope: trusted
          key: ${{ needs.hash_pr.outputs.files_hash }}
          version: 2
          restore-keys: |
            linux-x64/
          path: examples/docker-cache/.cache/docker-buildx
          strict: false

      - name: Assert exact asset cache hit - trusted
        if: ${{ steps.cache-trusted.outputs.matched-key != '' && startsWith(steps.cache-trusted.outputs.matched-key, 'trusted/') }}
        shell: bash
        env:
          ZIZMOR_VALUE_16: ${{ steps.cache-trusted.outputs.cache-hit }}
          ZIZMOR_VALUE_17: ${{ steps.cache-trusted.outputs.matched-key }}
          ZIZMOR_VALUE_18: ${{ github.repository }}
          ZIZMOR_VALUE_19: ${{ needs.hash_pr.outputs.files_hash }}
        run: |
          test "$ZIZMOR_VALUE_16" = "true"
          test "$ZIZMOR_VALUE_17" = "trusted/$ZIZMOR_VALUE_18/main/docker/linux-x64/$ZIZMOR_VALUE_19/v2"

      - name: Restore Docker cache asset - shared
        id: cache-shared
        uses: ./
        with:
          repository: ${{ env.CACHE_REPOSITORY }}
          cache-name: docker
          scope: shared
          key: ${{ needs.hash_pr.outputs.files_hash }}
          version: 2
          restore-keys: |
            linux-x64/
          path: examples/docker-cache/.cache/docker-buildx
          strict: false

      - name: Assert exact asset cache hit - shared
        if: ${{ steps.cache-shared.outputs.matched-key != '' && startsWith(steps.cache-shared.outputs.matched-key, 'shared/') }}
        shell: bash
        env:
          ZIZMOR_VALUE_20: ${{ steps.cache-shared.outputs.cache-hit }}
          ZIZMOR_VALUE_21: ${{ steps.cache-shared.outputs.matched-key }}
          ZIZMOR_VALUE_22: ${{ github.repository }}
          ZIZMOR_VALUE_23: ${{ needs.hash_pr.outputs.files_hash }}
        run: |
          test "$ZIZMOR_VALUE_20" = "true"
          test "$ZIZMOR_VALUE_21" = "shared/$ZIZMOR_VALUE_22/docker/linux-x64/$ZIZMOR_VALUE_23/v2"

      - name: Restore Docker cache asset - untrusted
        id: cache-untrusted
        uses: ./
        with:
          repository: ${{ env.CACHE_REPOSITORY }}
          cache-name: docker
          scope: untrusted
          key: ${{ needs.hash_pr.outputs.files_hash }}
          version: 2
          restore-keys: |
            linux-x64/
          path: examples/docker-cache/.cache/docker-buildx
          strict: false

      - name: Assert exact asset cache hit - untrusted
        if: ${{ steps.cache-untrusted.outputs.matched-key != '' && startsWith(steps.cache-untrusted.outputs.matched-key, 'untrusted/') }}
        shell: bash
        env:
          ZIZMOR_VALUE_24: ${{ steps.cache-untrusted.outputs.cache-hit }}
          ZIZMOR_VALUE_25: ${{ steps.cache-untrusted.outputs.matched-key }}
          ZIZMOR_VALUE_26: ${{ github.repository }}
          ZIZMOR_VALUE_27: ${{ github.event.pull_request.number }}
          ZIZMOR_VALUE_28: ${{ needs.hash_pr.outputs.files_hash }}
        run: |
          test "$ZIZMOR_VALUE_24" = "true"
          test "$ZIZMOR_VALUE_25" = "untrusted/$ZIZMOR_VALUE_26/pr-$ZIZMOR_VALUE_27/docker/linux-x64/$ZIZMOR_VALUE_28/v2"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Build using restored cache
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: examples/docker-cache
          file: examples/docker-cache/Dockerfile
          tags: cache-the-planet-example:restored
          push: false
          outputs: type=cacheonly
          cache-from: type=local,src=examples/docker-cache/.cache/docker-buildx
