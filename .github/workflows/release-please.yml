name: Release Please

on:
  workflow_dispatch:
  push:
    branches:
      - main

concurrency:
  # Release Please creates/updates release metadata and must not race with
  # generated-dist or manifest commits on the default branch.
  group: cache-the-planet-main-writes
  cancel-in-progress: false

jobs:
  release-please:
    name: Create release PR or GitHub release
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      - name: Create AstraGermany-Bot token
        id: bot-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          owner: Ludy87
          repositories: cache-the-planet
          permission-contents: write
          permission-issues: write
          permission-pull-requests: write

      - name: Release Please
        id: release
        uses: googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7 # v5.0.0
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
          token: ${{ steps.bot-token.outputs.token || github.token }}

      - name: Check out release commit
        if: steps.release.outputs.release_created == 'true'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ steps.release.outputs.sha }}
          token: ${{ steps.bot-token.outputs.token || github.token }}
          persist-credentials: true

      - name: Update floating major and minor tags
        if: steps.release.outputs.release_created == 'true'
        env:
          RELEASE_MAJOR: ${{ steps.release.outputs.major }}
          RELEASE_MINOR: ${{ steps.release.outputs.minor }}
          TOKEN: ${{ steps.bot-token.outputs.token || github.token }}
        run: |
          set -euo pipefail

          git config user.name "AstraGermany-Bot[bot]"
          git config user.email "AstraGermany-Bot[bot]@users.noreply.github.com"
          git remote add gh-token "https://$TOKEN@github.com/Ludy87/cache-the-planet.git"
          git tag -d "v$RELEASE_MAJOR" || true
          git tag -d "v$RELEASE_MAJOR.$RELEASE_MINOR" || true
          git push origin ":v$RELEASE_MAJOR" || true
          git push origin ":v$RELEASE_MAJOR.$RELEASE_MINOR" || true
          git tag -a "v$RELEASE_MAJOR" -m "Release v$RELEASE_MAJOR"
          git tag -a "v$RELEASE_MAJOR.$RELEASE_MINOR" -m "Release v$RELEASE_MAJOR.$RELEASE_MINOR"
          git push origin "v$RELEASE_MAJOR"
          git push origin "v$RELEASE_MAJOR.$RELEASE_MINOR"
